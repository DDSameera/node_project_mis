const express = require('express');
const router = express.Router();

const userRoute = require('./user.route');
const prodCatRoute = require('./productCat.route');

router.use
module.exports = router;